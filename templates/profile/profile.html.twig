{% extends 'base.html.twig' %}

{% block title %}Профиль{% endblock %}

{% block body %}

    <h1>Профиль</h1>
    <h2>{{ user.nickname }}</h1>
    <h2>{{ user.email }}</h1>
    <h2>{{ user.isVerified }}</h1>

{% endblock %}
